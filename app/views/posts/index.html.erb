<% @posts.each do |post| %>
  <!-- <div><%= post.title %></div> -->
  <!-- <div><a href='<%= "/posts/#{post.id}" %>'><%= post.title %></a></div> -->
  <div><%= link_to post.title, "/posts/#{post.id}" %></div>

<% end %>
